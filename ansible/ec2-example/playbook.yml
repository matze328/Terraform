- name: Install Nginx on web servers
  hosts: servers
  become: yes
  tasks:
     - name: Update apt cache
       apt:
        update_cache: yes

     - name: Install Nginx on webservers
       apt:
        name: nginx
        state: present

    